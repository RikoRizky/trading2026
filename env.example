# ============================================
# Supabase Configuration (REQUIRED)
# ============================================
# Get these values from your Supabase Dashboard:
# 1. Go to https://app.supabase.com
# 2. Select your project
# 3. Go to Settings > API
# 4. Copy "Project URL" → NEXT_PUBLIC_SUPABASE_URL
# 5. Copy "anon public" key → NEXT_PUBLIC_SUPABASE_ANON_KEY
#
# These are required for authentication and database access.
# Without these, the middleware will skip authentication checks.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Site URL for Production (REQUIRED for production)
# Set this to your production domain, e.g., https://your-app.vercel.app
# This is used for email confirmation and password reset links
# For local development, leave empty or use http://localhost:3000
# For production, set this to your actual domain
NEXT_PUBLIC_SITE_URL=https://your-app.vercel.app

# Admin Configuration (Optional - untuk sistem lama)
# NEXT_PUBLIC_OWNER_EMAIL=admin@example.com

# Database Configuration
DATABASE_URL=your_database_url_here

# Next.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_here

# Midtrans Configuration (untuk premium membership)
MIDTRANS_SERVER_KEY=your_midtrans_server_key
MIDTRANS_CLIENT_KEY=your_midtrans_client_key

# Stripe Configuration (untuk premium membership) - Optional
STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key
STRIPE_SECRET_KEY=your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret

# Email Configuration (untuk notifikasi)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

